server {
  listen 80;
  server_name _;
  root /var/www/html;
  index index.html;

  # SPA-роутинг; API не трогаем — его проксирует внешний nginx
  location / {
    try_files $uri $uri/ /index.html;
  }
}
